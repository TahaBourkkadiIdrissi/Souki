
# Dependances directes uniquement : pip resout les transitives
# (starlette, pydantic-core, anyio, click, h11, ecdsa, rsa, six, etc.).
# Des bornes basses (>=) evitent les erreurs "No matching distribution found"
# que provoquaient les pins exacts de versions inexistantes.

# --- API ---
fastapi>=0.115.0
uvicorn>=0.34.0
python-multipart>=0.0.18   # uploads/formulaires FastAPI ; <0.0.18 vulnerable (CVE-2024-53981)
email-validator>=2.0.0     # requis par pydantic EmailStr
pydantic>=2.10.0

# --- Base de donnees ---
SQLAlchemy>=2.0.36
psycopg2-binary>=2.9.10    # 2.9.10 = premiere version avec wheels Python 3.13
greenlet>=3.1.0            # requis par SQLAlchemy

# --- Auth / securite ---
python-jose>=3.4.0         # >=3.4.0 corrige CVE-2024-33663 / CVE-2024-33664
bcrypt>=4.2.0
google-auth>=2.38.0        # verification des id_token Google (login OAuth)
requests>=2.32.4           # requis par google.auth.transport.requests

# --- Services ---
google-genai>=2.0.0,<3.0   # SDK Gemini (api/algorithms.py) ; 0.2.0 exigeait pillow<12 (conflit CVE)
apscheduler>=3.10.4,<4.0
phonenumbers>=8.13.0
resend>=2.10.0             # envoi des emails OTP (supporte les cles d'idempotence)
pillow>=12.2.0             # re-encodage des images uploadees ; >=12.2 corrige les CVE 2026
python-dotenv>=1.0.1

# --- Tests ---
httpx>=0.27.0              # requis par fastapi.testclient.TestClient
pytest>=8.3.0


